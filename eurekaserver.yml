server:
  port: 8070

eureka:
  instance:
    hostname: srieurekaserver # Use container name
  client:
    fetchRegistry: false
    registerWithEureka: false
    serviceUrl:
      defaultZone: http://srieurekaserver:8070/eureka/

    
